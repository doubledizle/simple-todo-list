<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todo List App</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Rubik&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css"
  integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay" crossorigin="anonymous">
  <link rel="stylesheet" type="text/css" href="/style.css">

</head>
<body>
  <section class="todo-container"> <!-- primary container -->
    <h2>To Do List</h2> <!-- this is the header -->
    <section class="todo"> <!-- main sectino that holds the form and list items -->
      <form action="/" method="post" class="todo-header"> <!-- form container -->
        <input type="text" name="title" value="title"> <!-- input area-->
        <textarea rows="6" cols="45" name="content">content</textarea> <!-- text body area-->
        <button type="submit"><span class="fas fa-plus"></span>Add Text</button> <!-- submit button-->
      </form>
      <ul class="todo-list">
        <% todoTasks.forEach(task => {%> <!-- pull each task entry and display it as follows -->
        <li class="todo-list-item"> <!-- each item is a 'list item' element-->
          <div class="todo-list-item-title"> <!-- div to apply title style-->
            <%= task.title %> <!-- this is the title entry in the DB -->
          </div>
          <div class="todo-list-item-content"> <!-- div to apply content style-->
            <%= task.content %> <!-- this is the content entry in the DB -->
          </div>
            <a href="/edit/<%= task._id %>" class="edit"><span class="fas fa-edit"></span></a> <!-- edit button -->
            <a href="/remove/<%= task._id %>" class="remove"><span class="fas fa-times"></span></a> <!-- remove button -->
        </li>
        <% }) %> <!-- this caps off the EJS code -->
      </ul>
    </section>
  </section>
</body>
</html>